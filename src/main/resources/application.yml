spring:
  application:
    name: djh-ai-code
server:
  port: 8123
  servlet:
    context-path: /api
# springdoc-openapi????
springdoc:
  group-configs:
    - group: 'default'
      paths-to-match: '/**'
      packages-to-scan: org.test.djhaicode.controller
# knife4j???????????????
knife4j:
  enable: true
  setting:
    language: zh_cn